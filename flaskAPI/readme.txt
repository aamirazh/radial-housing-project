the instructions for setting up flask on the vm are on the help page of the course site

once set up, run app.py in the VM and view localhost:5000 in a browser in the vm to see the results

<distance> is one of: underFive, fiveToTen, tenToTwenty, overTwenty
